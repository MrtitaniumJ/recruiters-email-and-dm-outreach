# Copy to .env and fill in. Never commit .env.

# Notion (cold email + shared with DM tools if you use NOTION_TOKEN there)
NOTION_TOKEN=
# Outreach recipients database id (or omit and use db_id.txt locally)
NOTION_DB_ID=

# Gmail SMTP (app password, not your normal password)
EMAIL_USER=
EMAIL_PASS=
